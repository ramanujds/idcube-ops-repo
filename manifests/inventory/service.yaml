apiVersion: v1
kind: Service
metadata:
  name: part-inventory-service
#  namespace: dev
  labels:
    app: part-inventory-service
spec:
  selector:
    app: part-inventory-service
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
